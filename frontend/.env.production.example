# Arquivo de exemplo para produção
# Copie para .env.production e preencha com os valores reais
# NUNCA commite .env.production no git!

# URL base da API — em produção usa /api (proxiado pelo Nginx)
VITE_API_URL=/api
